extends base


block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="Username", name="username", type="text", required)
        input(placeholder="Password", name="password", type="password", required)
        input(type="submit", value="Login")
        br
        a(href="https://github.com/login/oauth/authorize?client_id=a54f0ad7385128844a1f&scope&allow_signup=false&scope=user:email") Continue with Githyub &rarr;

    hr      
    div 
        span Don't havve an account?
        a(href="/join") Create one now &rarr;